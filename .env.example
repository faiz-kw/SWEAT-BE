# Django Secret Key (generate a strong key for production)
SECRET_KEY=04g1u)az6^omy!7+e4+y6g#7na396=&^laj#d2t@dnqog)(znz

# Debug mode (True for development, False for production)
DEBUG=True

# Allowed Hosts (comma-separated)
ALLOWED_HOSTS=localhost,127.0.0.1

# CORS Allowed Origins (Vite frontend ports)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173

# Database configuration
# Leave DATABASE_URL empty to use default local SQLite for development
# For PostgreSQL (production / NeevCloud):
# DATABASE_URL=postgres://user:password@localhost:5432/fitness_command_center
DATABASE_URL=

DEBUG=True
# JWT Settings
JWT_ACCESS_TOKEN_LIFETIME_MINUTES=15
JWT_REFRESH_TOKEN_LIFETIME_DAYS=7
